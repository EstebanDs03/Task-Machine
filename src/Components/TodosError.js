import React from 'react';

// Define el componente TodosError, que representa un mensaje de error para la lista de tareas.
function TodosError() {
  return (
    // Renderiza un párrafo con el mensaje de error.
    <p>Error...</p>
  );
}

// Exporta el componente TodosError para que pueda ser utilizado en otros archivos.
export { TodosError };
